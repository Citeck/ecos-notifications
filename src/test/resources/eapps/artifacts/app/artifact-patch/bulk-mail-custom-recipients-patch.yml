id: bulk-mail-custom-recipients-patch

name:
  ru: Добавление тестовых провайдеров

target: app/config$bulk-mail-custom-recipients-providers

type: json
config:
  operations:
    - op: set
      path: '$.value'
      value:
        - asd
        - zxc
